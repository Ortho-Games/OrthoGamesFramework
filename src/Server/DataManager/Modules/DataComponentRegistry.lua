local ReplicatedStorage = game:GetService("ReplicatedStorage")

local GenerateArrayRegistry =
	require(ReplicatedStorage.Shared.ComponentRegistry.GenerateArrayRegistry)

return GenerateArrayRegistry()
